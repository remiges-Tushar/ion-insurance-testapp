routingRules:
  - version: "2.0.0"
    targetType: "url"
    target:
      url: "${BPP_URL}/webhook"
    endpoints:
      - discover
      - select
      - init
      - confirm
      - status
      - cancel
      - rate
      - support
      - on_publish
      - reconcile
